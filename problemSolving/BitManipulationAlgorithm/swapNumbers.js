// npm run swapNumbers
// โจทย์:
// ให้เขียนฟังก์ชันที่รับค่าตัวเลขสองตัว และคืนค่าตัวเลขที่ถูกสลับกัน
//
// Input: a, b คือจำนวนเต็มสองตัว
// Output: array ที่ประกอบด้วยตัวเลขที่ถูกสลับกัน
//
// ตัวอย่าง Input และ Output:
// Input: a = 3, b = 5
// Output: [5, 3]

function swapNumbers(a, b) {

}

module.exports = swapNumbers;

// ตัวอย่างที่ 1
const [a1, b1] = swapNumbers(3, 5);
console.log([a1, b1]); // Output: [5, 3]

// ตัวอย่างที่ 2
const [a2, b2] = swapNumbers(-1, 10);
console.log([a2, b2]); // Output: [10, -1]